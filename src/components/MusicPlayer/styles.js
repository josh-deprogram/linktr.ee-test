import styled from "styled-components";
import { COLORS } from "../../config";

export const MusicPlayerContainer = styled.div`
	width: 100%;
	background: ${COLORS.BLUE_GREY_4};
`;

export const MusicPlayerImg = styled.img`
	position: relative;
	width: 100%;
	max-width: 320px;
`;
